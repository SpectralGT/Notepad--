import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.SpectralGT.NotePadMinusMinus',
  appName: 'Notepad--',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
